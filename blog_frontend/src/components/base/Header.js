import styled from 'styled-components';

const StyledHeader = styled.div`
`;

const Header = () => {
    return (
        <StyledHeader>
            AuthForm
        </StyledHeader>
    );
}

export default Header;